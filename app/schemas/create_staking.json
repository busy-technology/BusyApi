{
  "definitions": {},
  "type": "object",
  "title": "Create Staking Request Schema",
  "required": ["walletId", "credentials"],
  "additionalProperties": false,
  "properties": {
    "walletId": {
      "type": "string",
      "title": "Wallet ID",
      "pattern": "^B-[a-zA-Z0-9]+$",
      "maxLength": 100
    },
    "credentials": {
      "$ref": "Credentials.json#/definitions/Credentials"
    }
  }
}
